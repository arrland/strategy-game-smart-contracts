{
  "name": "strategy-game-smart-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for the strategy game",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1"
  }
}
